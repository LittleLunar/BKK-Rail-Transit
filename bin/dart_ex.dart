import 'package:dart_ex/system/Main.dart';
import 'package:dart_ex/Station.dart';
import 'package:dart_ex/PathSolution.dart';

// Depth First Search
void main(List<String> arguments) async {
  
  // var returner = {};
  // returner["a"] = Station(name: "a", lineNumber:1);
  // if(!returner.containsKey('b')) {
  //   returner["b"] = Station(name:"b", lineNumber:1);
  // } 
  // returner['a'].addAdjacency(returner['b']);

  // print(returner);
  // print(returner['a']);
  // print(returner['b']);
  // print(identical(returner['a']?.getAdjacencies['b'], returner['b']));

  // Map<String, Station> data = await StationBuilder.loadFromJSON('./db/station/Station.json');
  // Map<String, Station> data = StationBuilder.getStationDemo();

  // print(data);
  
  

}

